# 麦麦「求知闭环」设计方案（交付版）

> 版本：v3 交付版 · 面向：任何需要理解并实现本方案的人或 AI
> 前置说明：本文档自包含，不依赖原始对话上下文。若你是第一次接触本方案，
> 从第 1 节「要解决的问题」读起即可完全理解。

---

## 1. 要解决的问题

麦麦（MaiBot，一个部署在 QQ 群的 AI 聊天角色）存在一个能力缺口：

**麦麦只会"被动应答"，不会"主动求知"。**

具体缺口有三条：

1. **不会发现自己不知道什么**：群里出现一个新词 / 新梗（黑话），麦麦没有机制判断"这个词我没学过、该问一下"。
2. **学会了也记不住**：即使问了、被群友解释了，现有的黑话学习系统有一个硬门槛——一个词要出现达到阈值 count（≥4/8/25/100）才会被自动挖掘并推断词义。只出现一两次的词，永远学不会。
3. **记忆存了但用不上**：麦麦有长期记忆系统（a-memorix），数据在积累，但存进去的内容没有明确通道回到对话决策里。

**一句话目标：**让麦麦遇到不认识的高价值词，能主动发现缺口 → 自行查询形成候选 → 向群友求证 → 把学到的词义以"多义并存"的方式永久记住，且不破坏麦麦原有的记忆结构。

---

## 2. 麦麦现有架构（必须理解的背景）

在动任何设计前，必须先认清麦麦已有的能力，避免重复造轮子，更避免破坏已有系统。

### 2.1 麦麦是什么

MaiBot 是一个开源聊天 AI，部署在 QQ（也支持其他平台），核心是"Maisaka"对话引擎。它像一个"群友"，有记忆、会学习群里的黑话、能主动参与聊天。

### 2.2 关键存储（本次方案的边界）

| 存储 | 内容 | 结构 | 是否可动 |
|---|---|---|---|
| `a-memorix` | 长期记忆：359 段文本、198 个实体、176 条事实主张 | 知识图谱 + 向量库 | **不可动**（麦麦核心资产） |
| `jargons` 表 | 黑话库，约 3800 条，844 条已启用 | 单值 meaning 结构 | **不可动**（被 jargon_miner 维护） |
| `mai_messages` | 全部聊天消息，约 1.9 万条 | 每行带 user_id / 时间戳 / 文本 | 只读（作为统计信号源） |

**设计铁律：本次方案新增的存储独立于以上三者，并永远不向 a-memorix 和 jargons 写入。**

原因：
- `a-memorix` 是麦麦"记住世界"的核心，改动它有不可逆风险
- `jargons` 由 `jargon_miner` 管理，它有一套自己的成本控制逻辑（见 §5.3）
- 我们的新知识是被"主动求证"来的，与自动挖掘是两个不同来源，天然应该分开

这项设计给了我们一个重要红利：**新系统不想要时，整块删除即可，不影响麦麦任何原有关己。**

### 2.3 麦麦已有的工具集（本次方案的复用点）

麦麦暴露给 Planner（决策层）的内置工具（`src/maisaka/builtin_tool/`）：

- `query_memory`：检索长期记忆（**只读**）
- `query_person_profile`：查人物画像（**只读**）
- `reply` / `send_emoji` / `send_image`：发消息
- `fetch_history` / `switch_chat` / `tool_search` / `view_forward_message` / `wait`

以及已装的搜索插件（如 `gomico_anysearch-plugin`）：
- `anysearch_search`：实时网页搜索，返回文本会作为 `planner_tool_result` 进入对话流

**关键结论：**
- 麦麦能查记忆、能发消息、能搜网页，但**没有"写记忆"的工具**，也没有"主动发起求知"的工具。
- 搜索插件的返回文本能被黑话学习流程读取（`planner_tool_result` 在允许的 learning source 清单里），所以"外部查询 → 形成候选"这条链路天然是通的。

### 2.4 麦麦现在"学黑话"的方式（及它的瓶颈）

`jargon_miner.py` 从聊天里自动挖词，流程是：
- 先检测词的**出现次数 count**
- 每跑一次词义推断要 **3 轮 LLM 调用**（inference1 → compare → finalize，temperature 0.3）
- `count >= 阈值(JARGON_INFERENCE_THRESHOLDS=[4,8,25,100])` 才触发推断

**瓶颈：** 一只出现一两次的词，count 到不了 4，永远不被挖掘。这正是"学会了记不住"的根源。
**门槛的深意：** 它是**成本控制**——词频不够时不值得花三次大模型调用去推断一个词义。
**本方案的立场：** 我们的求知闭环**不走这个门槛，也不修改它**。因为求知是"主动求证"，答案由群友直接提供，不需要再花三轮 LLM 从零推断词义。两条路径并行：jargon_miner 继续自动挖高频词（保住成本控制），求知闭环处理主动问来的词（零推断成本）。两者不冲突。

---

## 3. 核心链路（六段）

```
识别 → 采集观察 → 价值算法 → 单发求证 → 多义落库 → 节流回退
```

1. **识别**：对话中捕捉陌生词 / 群友用了麦麦不理解的词（通过 `check_knowledge` 自检）
2. **采集观察**：陌生词先入**观察池**，只记录（词 / 首次出现时间 / 语境），**不立刻问**
3. **价值算法**：用多因子评估该词是否值得学，价值达标才进入待询问
4. **单发求证**：从待询问里选当前价值最高的一个词去问；问完隔一段时间再评估下一个
5. **多义落库**：把学到的词义以多条 sense 并存写入**独立的三层结构**
6. **节流回退**：同词 cooldown、置信度衰减、语义漂移检测（词义变了则未知度回升）

三个全部内置工具：

```
check_knowledge(term) → { known, senses[], entities[] }
    · 查三层结构 → jargons → a-memorix，全未命中返回 known=false 并投入观察池

ask_group(term, candidate) → 发消息向群友求证（可附候选解释让群友一句话指正）

remember_term(term, senses[], context) → 把多条义写入独立三层结构
```

---

## 4. 数据模型：三层解耦（核心设计）

词义不是"一个词一个含义"，而是"一个词挂多个无上限的含义，靠上下文现选"。
这解决了人类的真实语义弹性：例如「大姨妈」可同时指 人 / 亲属关系 / 生理期，三者并存不冲突。

### 4.1 四个新表

```sql
-- 1) 词位表：只存"词"本身，不含任何含义
CREATE TABLE terms (
    id          INTEGER PRIMARY KEY,
    term        TEXT NOT NULL UNIQUE,      -- 词面，如 '大姨妈'
    term_type   TEXT DEFAULT 'term',       -- term / nickname / alias
    first_seen  TEXT,
    last_seen   TEXT
);

-- 2) 词义表：一个词挂多个义，数量不限，每行一个义
CREATE TABLE term_senses (
    id          INTEGER PRIMARY KEY,
    term_id     INTEGER NOT NULL REFERENCES terms(id),
    sense       TEXT NOT NULL,             -- 该词的一个含义
    source      TEXT NOT NULL,             -- anysearch / 群友昵称 / 人工 / 记忆
    context_note TEXT,                     -- 适用语境
    confidence  REAL DEFAULT 0.5,          -- 置信度
    evidence_ref TEXT,                     -- 证据来源链接/消息id
    created_at  TEXT,
    FOREIGN KEY (term_id) REFERENCES terms(id)
);
CREATE INDEX idx_sense_term ON term_senses(term_id);

-- 3) 实体表：同义词同指一人/事，是"语义汇聚点"
CREATE TABLE entities (
    id          INTEGER PRIMARY KEY,
    name        TEXT NOT NULL,
    entity_type TEXT DEFAULT 'person',     -- person / game / show / concept
    note        TEXT
);

-- 4) 词义-实体关联：把每个词义绑到它实际指代的实体（可空）
CREATE TABLE sense_entities (
    sense_id   INTEGER NOT NULL REFERENCES term_senses(id),
    entity_id  INTEGER NOT NULL REFERENCES entities(id),
    PRIMARY KEY (sense_id, entity_id)
);
```

### 4.2 数据示例

```
terms:                '大姨妈'
term_senses:          3 行
  · sense: 群友阿琳的昵称   source: 群友:雪冰冰   context: 群里@大姨妈
  · sense: 母亲的姐妹       source: anysearch    context: 谈家庭
  · sense: 女性月经俗称      source: anysearch    context: 日常闲聊

同义词同指（多词面 → 一实体）:
  terms: ball哥, 虬, 黄靖虬   →   entities: 黄靖虬(person)
  通过 sense_entities 汇聚到同一实体
```

### 4.3 增量存储：观察池 / 待询问池

```sql
CREATE TABLE pending_terms (
    id            INTEGER PRIMARY KEY,
    term          TEXT NOT NULL,
    first_seen    TEXT,
    last_seen     TEXT,
    count         INTEGER DEFAULT 0,
    freq_per_hour REAL DEFAULT 0,          -- 固定窗口频率
    reuse_count   INTEGER DEFAULT 0,       -- 跨间隔复现次数
    speaker_count INTEGER DEFAULT 0,       -- 不同说话人数（扩散度）
    value_score   REAL DEFAULT 0,
    status        TEXT DEFAULT 'observation', -- observation/ready/asked/learned/dropped
    last_asked    TEXT,
    context_sample TEXT
);
```

status 流转：observation（观察）→ ready（价值达标待问）→ asked（问过）→ learned（已落库）或 dropped（弃）

---

## 5. 价值算法模块（核心）

目标：只问"真正值得学"的词，符合人类"隔阵子又出现、而且好多人都在用"才开口问的直觉。
应对生词洪峰：不批量问，选价值最高的单发。

### 5.1 多因子信号

| 因子 | 信号 | 数据来源 |
|---|---|---|
| 频次 | 出现总次数 count | 消息分词统计 |
| 频率 | 固定滑窗（默认 1h）内出现次数 | mai_messages 时间戳 |
| 跨时间复现 | 在时间窗口内再次出现（隔段又见） | last_seen 间隔 |
| 扩散度 | 多少个不同的人在用 | mai_messages.user_id 去重 |
| 活跃距离 | 最近一次距今多久（过久降权） | now - last_seen |
| 未知度 | 完全未知加分，已知减分 | check_knowledge 自检 |
| 语境显著 | 被@/强调/加引号（更可能是关键词） | 消息元数据 |

### 5.2 加权评分公式 + 各因子下级公式

每个因子**先归一化到 [0,1]**（解决量纲不同：次数/次每小时/时长/人数/布尔值不可直接相加），再乘权重求和。

```
value = (w1·norm(频次) + w2·norm(频率) + w3·norm(跨复现)
        + w4·norm(扩散度) + w5·norm(活跃度) + w6·norm(未知度)) · 100
```

**各因子下级公式（论文依据）：**

**① 频次 —— log 压缩长尾**
```
频次归一 = log(1 + count) / log(1 + max_count)
```
依据：Szabo & Huberman 2008，content popularity 的对数线性关系；社会化传播频次呈长尾分布，log 压平后 3 次与 100 次差距合理。

**② 频率 —— 固定滑动窗口**
```
频率 = 最近 ΔT（默认 1h）内的出现次数，窗口恒定
频率归一 = log(1 + 滑窗次数) / log(1 + 滑窗上限)
```
依据：固定窗口消除"count/观察时长"的分母不稳定问题（早期的短窗刷屏与长窗低频被错误拉平）；对齐 AD 算法的"激活"概念。

**③ 跨时间复现 —— 复用档位**
```
跨复现 = min(复用次数 / 复用上限, 1)
```

**④ 扩散度 —— 独立用户占比**
```
扩散度 = 用过该词的独立用户数 / 群活跃用户基数
```
依据：信息传播文献（Guille et al. 2013 综述；Bao et al. 2013 结构多样性），传播范围/深度与最终流行度强相关；用独立说话人数占比避免只靠一人刷屏误判。

**⑤ 活跃度 —— 指数时间衰减（半衰期）**
```
活跃度 = exp(-Δt / τ)，Δt = 距今时长，τ = 半衰期标度（可配）
```
依据：AD 算法将信息传播建模为"激活-衰减"动态过程（Wu et al. 2023, arXiv 2306.12159）。越久未出现越接近 0，惩罚陈梗。

**⑥ 未知度 —— 布尔/置信折减**
```
未知度 = 1（完全未知） / 1 - confidence（已有低置信 sense） / 0（已学）
```
依据：novelty 检测的"信息是否新"加权（Temporal IDF 等）。

**⑦ 语境显著 —— 强调/提及标记（辅助项）**
```
语境显著 = (被@ + 被引号包裹 + 被显式解释的次数) / 出现总次数
```

### 5.3 权重标定：熵权法（不靠拍脑袋）

```
对每个词计算它在各因子上的"信息熵"（数据离散度）
因子熵越小（区分度越高）→ 权重越大
w_i = (1 - e_i) / Σ(1 - e_j)
```

依据：熵权法（EWM, Shannon entropy）依据数据本身变异度客观定权，消除主观偏差。
实现：先在真实群数据上采样一批词算基准权重，再人工微调语义倍率。

### 5.4 真实数据验证（已做的实测）

用麦麦主群「启动超级孵蛋模式」12035 条消息、33 个独立用户实测，验证了公式方向正确：

| 词 | count | 近1h | 跨天 | 说话人 | 距今h | 验证点 |
|---|---|---|---|---|---|---|
| 麦麦 | 64 | 0 | 6 | 7 | 1.0 | 极高频但已知，须被未知度压住 |
| 天秤星 | 52 | 0 | 6 | 12 | 2.3 | 高频、通行广（本人），不应问 |
| 孵蛋 | 36 | 0 | 7 | 14 | 1.2 | 跨天最久+扩散多人，值得学 |
| 牛来 | 33 | 0 | 4 | 12 | **22.5** | 高频多人但已过气，被活跃度衰减抑制 |
| 马桶熊 | 8 | **3** | 2 | 3 | 0.5 | 低频但正在爆发，被滑窗捕获 |

**公式有效**：马桶熊近 1h 爆发被滑窗抓住（新流行冒头）；牛来过气被活跃度衰减正确压制。
**暴露必须补强的点**：未知度/置信度权重必须够强，才能压住"麦麦/天秤星"这类极高频但已知的词，防止麦麦去问"麦麦是什么意思"。

---

## 6. 语义漂移：未知度动态变化

一个词的意思会随时间变。麦麦需能发现"我的理解过期了"，从而未知度回升、重新求知。

### 6.1 现状确认

查源码确认：麦麦原生结构里**没有词义基线 / 词义漂移检测**。
- `expression_vector_index.py` 里的 baseline/drift 是 **embedding 模型档案兼容性检查**（验证向量模型换没换、余弦 ≥0.999），不是词义漂移。
- `attention_drift` 是**聊天话题漂移**（让麦麦话头偏一偏），也不是词义漂移。

所以这是全新能力，无历史包袱、无冲突。

### 6.2 方案：语义冲突为主（贴合群内私梗）

群内私梗的向量不可靠（"马桶熊""天秤星"在公共 embedding 里无意义），故优先用语义冲突信号：

**信号 A（词义漂移可感知）：**
当麦麦尝试把旧义套到新语境却"对不上"（如"大姨妈"原记成昵称，却出现在痛经语境），麦麦意识到旧义不适用 → 未知度回升。

**信号 B（群友直接纠正）：**
"不是啊，大姨妈是指生理期"——群友的纠正信息直接标记该词义漂移。

**触发条件：** 连续 N 次上下文不符（防单次误判），或单次强冲突（群友显式纠正）。

（技术选型：可选 ①向量漂移简单筛查 ②纯语义冲突 ③两者结合。本设计倾向②，因私梗为主场景下公共向量帮倒忙；如未来扩展到公共语义词再叠加①。）

---

## 7. 实现形态（建议：独立插件）

- 通过 MaiBot 插件运行时（`MaiBotPlugin`）实现，`@Tool` 注入三个工具 + 监听消息流做识别
- **不改主源码 `builtin_tool/`** → 规避升级被覆盖风险，可单独启停
- 数据存独立 sqlite（`knowledge.db`），完全隔离于 a-memorix / jargons
- 可行性参考：`gomico_anysearch-plugin` 已示范 `@Tool` 注入方式；jargon.system 等 hook 已存在，可在 planner 循环插入自检
- 风险 C 见 §8，需 PoC 实证插件 hook 能否插入"认知缺口检测"

---

## 8. 已知问题 / 风险（诚实清单）

| # | 问题 | 严重度 | 缓解 / 处理 |
|---|---|---|---|
| R1 | **未知度/置信度权重难标**：需压住"麦麦/天秤星"等高频已知词不重复问 | 高 | 熵权法客观标定 + 真实数据采样；设 cooldown 与"已问过"标记 |
| R2 | **打扰群友**：问多了吵群 | 中 | 价值算法把关，只问跨复现+多人的高价值词；单发 + cooldown；附候选让群友一句话指正 |
| R3 | **搜索词义 ≠ 群内词义**：网上解释与群内私梗含义不同 | 中 | 多义并存、不覆盖；群友义 source 权重更高；最终以群友为准 |
| R4 | **错过低频重要词**：只出现一次、未跨复现的词可能被弃 | 中 | 观察池长期保留 + 定期重扫；可降低阈值窗口 |
| R5 | **插件工具注入不确定**：能否在 planner 循环插入自检未实证 | 高 | 先做 M1 PoC 验证 `@Tool` 注入 + hook；失败则退回改主源码方案 |
| R6 | **词义漂移检测依赖麦麦/群友主动发现**：低频漂移可能漏检 | 中 | 记录"语义冲突"事件日志；必要时叠加向量漂移粗筛 |
| R7 | **分词/子串匹配粗糙**：本方案验证用子串近似，正式实现需中文分词准确率 | 中 | 引入分词（如 jieba，麦麦已装 pypinyin/jieba 依赖可用） |
| R8 | **新存储长期闲置增长**：多义 sense、观察池会持续累积 | 低 | 定期清理：低置信 + 长期未复现的 sense 标记可删除 |
| R9 | **直写绕过原生门槛的量疑**：绕过 jargon 门槛是否合理（普通用户视角担心） | 中 | 设计铁律：新系统独立存储、不写 jargons/a-memorix；本条已用 §2.4 说明，是并行而非替代 |
| R10 | **无法确定"语境显著"的判定粒度** | 低 | 用消息元数据（是否被@、引号）近似的折中；实现后可校准 |

---

## 9. 里程碑

- **M1 PoC**：验证插件能注入 tool + 读通三层结构 & 观察池（改插件，不影响麦麦正常运行）
- **M2**：`check_knowledge` / `ask_group` / `remember_term` 三工具可调用
- **M3**：识别 + 采集观察 + 价值算法跑通
- **M4**：单发求证 + 多义落库 + 复现窗口节流
- **M5**：语义漂移检测 + 置信度 + 回退 + 定期重扫打磨

---

## 10. 一句话总结

让麦麦具备"主动求知、随问随记、多义并存、感知词义变化"的能力，
用**完全独立的存储**实现，**不触碰、不修改、不破坏**麦麦原生的 a-memorix 记忆库与 jargons 黑话库，
从而在不引入不可逆风险的前提下，补齐麦麦从"被动应答"到"主动学习"的关键一跃。
